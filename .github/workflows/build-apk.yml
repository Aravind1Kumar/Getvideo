name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      buildType:
        description: "Build Type: assembleRelease or assembleDebug"
        required: false
        default: "assembleRelease"

jobs:
  build-apk:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Frontend Dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build Web Distribution
        working-directory: ./frontend
        run: npm run build

      - name: Copy Assets to Android
        working-directory: ./frontend
        run: npx cap copy android

      - name: Build Android APK
        working-directory: ./frontend/android
        run: |
          chmod +x gradlew
          TASK="${{ github.event.inputs.buildType || 'assembleRelease' }}"
          echo "Building Gradle task: $TASK"

          # If release build, generate a debug signing key so APK installs immediately on any phone
          if [[ "$TASK" == *"Release"* ]]; then
            echo "Generating signing key for release APK..."
            mkdir -p $HOME/.android
            keytool -genkey -v -keystore $HOME/.android/debug.keystore \
              -storepass android -alias androiddebugkey -keypass android \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "C=US, O=Android, CN=Android Debug"

            ./gradlew $TASK \
              -Pandroid.injected.signing.store.file="$HOME/.android/debug.keystore" \
              -Pandroid.injected.signing.store.password=android \
              -Pandroid.injected.signing.key.alias=androiddebugkey \
              -Pandroid.injected.signing.key.password=android \
              --stacktrace
          else
            ./gradlew $TASK --stacktrace
          fi

      - name: Locate and Rename APK
        run: |
          mkdir -p output_apk
          find frontend/android/app/build/outputs/apk -name "*.apk" -exec cp {} output_apk/AK-Downloader-PandaCompress.apk \;
          echo "Generated APK files:"
          ls -lh output_apk/

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AK-Downloader-PandaCompress-APK
          path: output_apk/AK-Downloader-PandaCompress.apk
          retention-days: 14
